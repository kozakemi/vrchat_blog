# 相册加密方案（AES-256-GCM + Zone 权限）实现文档

> 目标：在静态站点/前端项目中，实现“相册资源加密存储 + 用户按 Zone 解密访问”的机制；登录通过加载本地密钥文件（JSON），不同用户可持有不同 Zone 的 key，从而拥有不同照片访问权限。

## 1. 背景与范围

### 1.1 适用场景
- 相册资源（图片/视频/音频/任意二进制）以**密文**形式存放在仓库/CDN/GitHub Pages。
- 访问者在浏览器端通过“导入密钥文件”完成“登录”，前端使用 WebCrypto 对资源进行解密并展示。
- 通过 **Zone ID** 进行权限划分：同一站点内可存在多个 Zone（例如：公开区、好友区、核心区等），不同用户的密钥文件只包含其可访问 Zone 的 key。

### 1.2 非目标（重要限制）
- **静态站点无法阻止密文被下载/缓存**：任何人都能下载到密文文件；安全性来自“没有 key 无法解密”。
- **无法在纯前端强制在线鉴权**：本方案不包含服务器端身份系统；“登录”仅表示用户持有正确密钥材料。
- **分享密钥文件 = 分享权限**：密钥文件泄露会导致对应 Zone 全量泄露；需通过流程/管理控制。

## 2. 术语

- **Zone**：权限分区单位。每个 Zone 对应一个对称密钥 `zoneKey`（32 bytes）。
- **zoneId**：Zone 唯一标识符（字符串），例如 `public`、`friends-2026`、`vrc-core`。
- **Key File（密钥文件）**：用户登录使用的 JSON 文件，包含 `username` + 可访问的 Zone 列表及其 key。
- **Asset（资源）**：单个照片/视频等二进制文件。
- **Manifest（清单）**：描述资源元数据、其所属 Zone、密文位置、加密参数等的 JSON。

## 3. 总体设计

### 3.1 数据流概览
1. **离线创建 Zone**：工具生成 `zoneId` 与 32 字节随机 key（AES-256-GCM）。
2. **离线加密资源**：工具对每个资源生成随机 `nonce(IV)`，用 `AES-256-GCM(zoneKey, nonce)` 加密，输出密文文件与清单。
3. **部署**：将密文资源 + 清单发布到站点（GitHub Pages/CDN）。
4. **登录**：用户在前端导入 key file（JSON）。
5. **解密展示**：前端根据清单中资源的 `zoneId` 选择对应 key，通过 WebCrypto 解密并渲染。

### 3.2 目录建议
（可按项目习惯调整，关键是密文与清单可被静态托管）
```
public/
  albums/
    manifest.json
    assets/
      <assetId>.bin        # 密文（建议统一扩展名，避免被当作图片直接处理）
      <assetId>.json       # 可选：单资源元数据（也可合并进 manifest）
```

## 4. 密码学方案

### 4.1 算法选择
- **加密**：AES-256-GCM
  - key：256 bit（32 bytes）
  - nonce/IV：96 bit（12 bytes，随机生成）
  - tag：128 bit（16 bytes，GCM 标准）
- **随机数**：CSPRNG（Node.js `crypto.randomBytes` / WebCrypto `getRandomValues`）
- **可选哈希**：SHA-256（用于完整性校验/去重/调试；非必须）

### 4.2 AAD（Additional Authenticated Data）
为绑定密文与上下文，建议启用 AAD，至少包含：
- `schemaVersion`
- `zoneId`
- `assetId`
- `mime`

AAD 的目的：
- 防止“跨 Zone/跨资源替换”导致的误解密或内容错配（GCM 的鉴别标签将覆盖 AAD）。

建议 AAD 采用 UTF-8 JSON 串（稳定字段顺序），例如：
```json
{"v":1,"zoneId":"friends-2026","assetId":"a_01H...","mime":"image/jpeg"}
```

### 4.3 密文封装格式（推荐）
为了简化前端解密，建议采用“**分字段存储在 manifest**，密文文件只存 ciphertext(+tag)”：
- `nonce`（base64）存 manifest
- `ciphertext`（二进制，含 GCM tag）存 `<assetId>.bin`

原因：
- WebCrypto `subtle.decrypt` 需要 nonce 与 ciphertext；tag 可与 ciphertext 合并（WebCrypto/多数实现使用“ciphertext||tag”）。

## 5. Zone 与访问控制模型

### 5.1 Zone Key 生成规则
- 每个 Zone 生成 **独立随机** 32 bytes key
- key 仅分发给需要访问该 Zone 的用户（通过密钥文件）

### 5.2 权限判定
资源所属 `zoneId` 决定解密 key：
- 用户 key file **包含该 zoneId 的 key** → 可解密
- 否则 → 不可解密（前端应隐藏/置灰/提示“无权限”）

### 5.3 密钥轮换（可选但建议）
轮换方式：
1. 生成新 key（同 zoneId 或新 zoneId）
2. 重新加密该 Zone 的资源
3. 更新用户 key file

注意：在静态部署场景，旧密文与旧 key 存在时，旧 key 泄露依然可解密旧密文；轮换需要清理旧密文并让用户更新 key file。

## 6. 密钥文件（Key File）规范（JSON）

### 6.1 文件结构（v1）
```json
{
  "schemaVersion": 1,
  "username": "alice",
  "zones": [
    {
      "zoneId": "friends-2026",
      "keyB64": "mS0xqkZ3cQk8y2n2fK7lqjVqg3wR0gq7bQkYb0lS0yM=",
      "comment": "好友相册"
    },
    {
      "zoneId": "public",
      "keyB64": "pWwWm0m4qU9Oe2h3m3vFqjZQdKzXW0tZfG6z5b4dQ1E="
    }
  ],
  "createdAt": "2026-04-27T12:00:00.000Z"
}
```

字段说明：
- `schemaVersion`：版本号，便于未来兼容
- `username`：显示用途/日志用途（不参与安全）
- `zones[]`：
  - `zoneId`：字符串
  - `keyB64`：Base64 编码的 32 bytes key
  - `comment`：可选备注

### 6.2 存储与分发建议
- **密钥文件不应上传到站点/仓库**，仅通过线下方式分发（私聊、加密网盘、U 盘等）。
- 建议用户将密钥文件放在本地安全位置；前端只在浏览器内存中使用，不写入 LocalStorage（除非用户明确允许）。

### 6.3 可选增强：密钥文件二次加密（后续）
如果需要“密钥文件本身也要密码保护”，可在 v2 引入：
- `kdf`: PBKDF2/Argon2（浏览器端 Argon2 成本更高）
- `salt`, `iterations`
- 使用用户口令派生 KEK（key-encryption-key）对 `zones[].keyB64` 进行 AES-GCM 加密

> 本文档先按“明文 JSON（受控分发）”实现。

## 7. Manifest（资源清单）规范

### 7.1 建议结构
`public/albums/manifest.json`：
```json
{
  "schemaVersion": 1,
  "generatedAt": "2026-04-27T12:00:00.000Z",
  "assetsBasePath": "/albums/assets/",
  "assets": [
    {
      "assetId": "a_01HY...7F",
      "zoneId": "friends-2026",
      "originalName": "IMG_1234.JPG",
      "mime": "image/jpeg",
      "size": 345678,
      "nonceB64": "oS3l9qjv0gq5gH7y",
      "cipherFile": "a_01HY...7F.bin",
      "aad": {"v":1,"zoneId":"friends-2026","assetId":"a_01HY...7F","mime":"image/jpeg"},
      "sha256B64": "optional"
    }
  ]
}
```

字段说明：
- `assetsBasePath`：方便前端拼接静态路径
- `nonceB64`：12 bytes nonce 的 base64
- `cipherFile`：密文文件名（建议只允许 `[a-zA-Z0-9._-]` 以避免路径注入）
- `aad`：用于前端重建 AAD；或直接存 `aadJson` 字符串（更稳定）

### 7.2 单资源元数据 vs 总清单
- 小规模：全部放到一个 `manifest.json` 足够
- 大规模：可按 Zone/日期拆分多个 manifest，前端按需加载

## 8. 创建工具（CLI）设计（Node.js/TypeScript）

### 8.1 目标能力
1. 创建 Zone（生成 key）
2. 生成/更新密钥文件（key file）
3. 加密照片/相册资源（产出密文 + manifest）

### 8.2 建议命令
假设 CLI 名为 `album-crypt`：

#### 8.2.1 创建 Zone
```bash
album-crypt zone create --zone-id friends-2026
```
输出：
- `zoneId`
- `keyB64`（给管理员用于写入密钥文件）

#### 8.2.2 创建/更新密钥文件
```bash
album-crypt keyfile create --username alice --out alice.key.json
album-crypt keyfile add-zone --keyfile alice.key.json --zone-id friends-2026 --key-b64 "<...>"
```

#### 8.2.3 加密资源并生成 manifest
```bash
album-crypt encrypt \
  --in ./photos \
  --out ./public/albums \
  --zone-id friends-2026 \
  --key-b64 "<...>" \
  --manifest ./public/albums/manifest.json
```

可选参数：
- `--glob "**/*.{jpg,jpeg,png,webp,mp4}"`
- `--keep-structure`：保留原目录结构（否则扁平化）
- `--dedupe`：基于 SHA-256 去重

### 8.3 实现要点（Node crypto / WebCrypto）

#### 8.3.1 生成 Zone Key（32 bytes）
```ts
import { randomBytes } from "node:crypto";

const key = randomBytes(32); // AES-256
const keyB64 = key.toString("base64");
```

#### 8.3.2 生成 nonce（12 bytes）
```ts
const nonce = randomBytes(12);
```

#### 8.3.3 AES-256-GCM 加密
Node 端可用 `createCipheriv("aes-256-gcm", key, nonce)`；建议把 tag 拼接到 ciphertext 末尾，便于前端 WebCrypto 直接解密：
```ts
import { createCipheriv } from "node:crypto";

function encryptAesGcm(key: Buffer, nonce: Buffer, plaintext: Buffer, aad: Buffer) {
  const cipher = createCipheriv("aes-256-gcm", key, nonce);
  cipher.setAAD(aad);
  const ciphertext = Buffer.concat([cipher.update(plaintext), cipher.final()]);
  const tag = cipher.getAuthTag(); // 16 bytes
  return Buffer.concat([ciphertext, tag]); // ciphertext||tag
}
```

#### 8.3.4 AAD 构建（稳定序列化）
确保字段顺序稳定（手工拼接或使用稳定 JSON 序列化库）：
```ts
const aadObj = { v: 1, zoneId, assetId, mime };
const aadJson = JSON.stringify(aadObj); // 需保证字段写入顺序固定
const aad = Buffer.from(aadJson, "utf8");
```

### 8.4 生成 assetId
建议使用：
- `nanoid` / `ulid` / `uuidv7`
要求：不泄露原文件名、可作为文件名安全使用。

### 8.5 输出产物
加密后输出：
- `public/albums/assets/<assetId>.bin`（密文）
- `public/albums/manifest.json`（清单，含 nonce、zoneId、mime 等）

## 9. 前端解密与展示（React + WebCrypto）

### 9.1 登录（导入密钥文件）
- UI：按钮“导入密钥文件（.json）”
- 读取方式：`<input type="file">` → `File.text()` → JSON.parse
- 内存存储：建议用 Zustand/store 保存 `Map<zoneId, CryptoKey>`

将 `keyB64` 导入为 WebCrypto CryptoKey：
```ts
function b64ToBytes(b64: string) {
  const bin = atob(b64);
  const bytes = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; i++) bytes[i] = bin.charCodeAt(i);
  return bytes;
}

async function importAesKey(keyB64: string) {
  const raw = b64ToBytes(keyB64);
  return crypto.subtle.importKey(
    "raw",
    raw,
    { name: "AES-GCM" },
    false,
    ["decrypt"]
  );
}
```

### 9.2 加载 manifest
```ts
const manifest = await fetch("/albums/manifest.json").then(r => r.json());
```

### 9.3 解密单个资源
```ts
async function decryptAsset({
  key,
  nonceB64,
  cipherUrl,
  aadJson
}: {
  key: CryptoKey;
  nonceB64: string;
  cipherUrl: string;
  aadJson: string; // 或从 aad 对象 JSON.stringify 得到
}) {
  const nonce = b64ToBytes(nonceB64);
  const cipherBuf = await fetch(cipherUrl).then(r => r.arrayBuffer());

  const plainBuf = await crypto.subtle.decrypt(
    { name: "AES-GCM", iv: nonce, additionalData: new TextEncoder().encode(aadJson) },
    key,
    cipherBuf
  );
  return plainBuf;
}
```

渲染图片：
```ts
const blob = new Blob([plainBuf], { type: mime });
const url = URL.createObjectURL(blob);
// <img src={url} />
```

### 9.4 无权限处理
若 `keyfile` 中不存在 `asset.zoneId`：
- 列表不展示该资源，或展示占位并提示“无权限”

## 10. 安全注意事项与最佳实践

1. **nonce 绝不能重复（同一 key 下）**  
   - 对每个资源随机生成 12 bytes nonce，概率上足够安全；不要用计数器除非严格保证不重复。
2. **key file 只在受控渠道分发**  
   - 可考虑把 key file 当作“实体钥匙”管理。
3. **不要把 key 写入 LocalStorage（默认）**  
   - 避免被同源脚本/扩展读取；如需“记住登录”，应显式提示并提供“清除密钥”。
4. **前端 XSS 风险**  
   - 一旦站点发生 XSS，攻击者可窃取内存中的 key 并解密资源；需严格 CSP、依赖审计、避免注入点。
5. **加密不是版权保护**  
   - 用户一旦解密即可截屏/保存明文；本方案主要用于访问控制与隐私隔离。

## 11. 版本管理与兼容性

建议在以下文件中都携带 `schemaVersion`：
- key file
- manifest
- AAD 里的 `v`

升级策略：
- 新版本增加字段时保持向后兼容（旧字段仍可工作）
- 破坏性变更（例如改封装格式）应提升主版本并提供迁移工具

## 12. 最小可落地实现清单（MVP）

### 12.1 CLI（Node/TS）
- [ ] `zone create`：生成 `zoneId + keyB64`
- [ ] `encrypt`：输入目录 → 输出密文 + manifest（支持 `--zone-id --key-b64`）
- [ ] 可选：`keyfile create / add-zone`

### 12.2 前端（React）
- [ ] 导入 key file（JSON）
- [ ] 加载 manifest 并按 Zone 过滤可见资源
- [ ] 解密并展示图片（先支持 jpeg/png/webp）
- [ ] “退出/清除密钥”按钮

---

如你希望我继续落地到代码层面（添加 CLI 工具项目、生成脚手架、并在本仓库里实现前端解密展示页面），告诉我你希望：
1) CLI 放在仓库根目录还是 `tools/album-crypt/`  
2) 加密产物放在 `public/albums/` 还是单独目录  
3) 相册 UI 需要的字段（时间、地点、描述、标签）有哪些

