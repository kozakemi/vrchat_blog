# 相册清单（manifest）、图片访问、加密与上传 — 实现说明

本文档总结当前仓库中与 **`manifest.json`、OSS 对象、函数计算签名、管理端加密上传** 相关的数据流与配置，便于部署与排错。算法级加密细节仍以《相册加密方案-实现文档》为准。

---

## 1. OSS 上的典型布局

| 对象键（示例） | 用途 |
|----------------|------|
| `albums/manifest.json` | 相册清单 JSON（默认；可通过环境变量改键名） |
| `albums/assets/<assetId>.bin` | 管理端「加密并上传」生成的密文对象 |

清单中的 **`assets`** 数组每一项描述一张资源；阅读页用其中的 **OSS 对象键** 向函数计算换临时 GET 链接（见下文「`file` 字段」）。

---

## 2. `manifest.json` 的形状与归一化

合并上传写入的根结构（与管理端 `mergeAndUploadManifest` 一致）大致为：

```json
{
  "schemaVersion": 1,
  "generatedAt": "<ISO8601>",
  "assetsBasePath": "/albums/",
  "assets": [ /* 多条 asset 记录 */ ]
}
```

前端拉取清单后会执行 **`normalizeAlbumManifestPayload`**（`src/lib/albumManifestFetch.ts`），兼容：

- 根节点即为 **`assets` 数组**（少见导出格式）→ 包装为 `{ schemaVersion: 1, assets }`
- 根上标准对象且含 **`assets`**
- 嵌套在 **`data.assets`** 下的写法

若缺少可用的 `assets` 数组，会抛出明确错误，而不是静默显示「0 张」。

---

## 3. 清单从哪里拉取（优先级）

实现入口：**`fetchAlbumManifestOrThrow`**（`src/lib/albumManifestFetch.ts`）。

1. **`VITE_ALBUM_MANIFEST_URL`**（须为完整 `http(s)://...`）  
   - 无效值（如单独一个 `/`）会被忽略，避免 `URL` 构造报错。  
   - 若设置且合法：**优先**对该 URL 发起 `fetch`，再按上文归一化 JSON。

2. **函数计算签名（与图片同一套 GET 签名）**  
   - 条件：存在签名服务根地址（默认内置 `VITE_OSS_SIGN_ENDPOINT` 的默认值，见 `src/lib/ossSignFetch.ts`）。  
   - 请求格式：`GET <签名服务>?file=<对象键>`  
   - 对象键默认 **`albums/manifest.json`**，可通过 **`VITE_ALBUM_MANIFEST_FILE`** 覆盖（须与 OSS 上真实路径一致，例如根目录清单则为 `manifest.json`）。  
   - 响应 JSON 中 **`signedUrl`** 为 OSS 临时 GET 地址；再 `fetch` 该地址得到清单正文。

3. **直链 / 本地回退**（无上述「有效直链」且签名链不可用时）  
   - 开发：`public` 下当前 dev server 的 `albums/manifest.json`  
   - 生产构建：相对站点同源的 `albums/manifest.json`（需随静态资源发布；否则易拿到 SPA 的 `index.html`）

---

## 4. 图片（及任意 OSS 对象）如何读：与清单相同的 GET 签名

阅读页 **`Album`**（`src/pages/Album.tsx`）对每条 `asset`：

1. 使用 **`asset.file`**（字符串）作为 OSS **对象键**。  
2. 调用 **`fetchSignedUrlForOssObject(file)`**（`src/lib/ossSignFetch.ts`）：  
   `GET <签名服务>?file=<encodeURIComponent(file)>`  
3. 解析返回的 **`signedUrl`**，经 **`resolveSignedUrlToAbsolute`**（`src/lib/ossSignedUrl.ts`）处理相对路径、错误指向 `localhost` 的绝对链接等，再请求 OSS 得到 `Blob`，转为 Object URL 供 `<img>` 使用。

签名结果带短期缓存（按对象键缓存，过期前复用）。

**重要（当前实现差异）：**  
管理端加密流水线写入清单字段为 **`cipherFile`**（指向 `albums/assets/xxx.bin`），而阅读页只读取 **`file`**。若清单中只有 `cipherFile` 而无 `file`，阅读页不会发起上图流程；且 **阅读页目前不对 `.bin` 密文做解密**，即便拿到字节也无法当普通图片渲染。若要让加密资源可被阅读端消费，需要后续在合并清单时写入与 FC 约定一致的 **`file`** 字段，或在 `Album` 中兼容 `cipherFile`，并实现与《相册加密方案》一致的解密逻辑。

---

## 5. 管理端：加密、合并清单、上传 OSS

页面：**`#/album-admin`**（`src/pages/AlbumAdmin.tsx`）。

### 5.1 加密

- 使用 **`encryptPlaintextToParts`**（`src/lib/albumCrypto.ts`），按 Zone 密钥与 AES-GCM 等规则生成密文与元数据（详见加密方案文档）。  
- 每条队列项会得到 **`row`**（含 `assetId`、`zoneId`、`nonceB64`、`cipherFile` 指向 OSS 键等）及 **`cipherBytes`**。

### 5.2 上传对象（PUT）

- **密文**：`resolvePutSignedUrl(ossKey, ossCfg, "application/octet-stream")` → **`putObjectWithSignedUrl`**，`Content-Type` 固定为 **`application/octet-stream`**（须与预签名一致）。  
- **清单**：合并后的 JSON → `resolvePutSignedUrl(MANIFEST_KEY, ossCfg, "application/json")`，`Content-Type` 为 **`application/json`**。

`resolvePutSignedUrl`（`src/lib/ossUpload.ts`）逻辑：

- 若用户在页面保存了 **OSS 网页 JSON**（RAM 等在浏览器侧签名）：仅用 **`ali-oss` 生成 PUT 预签名**。  
- 否则：请求签名服务的 **`?put=1&key=<对象键>`**（需在函数计算侧实现）。

### 5.3 合并清单

上传前会 **`tryFetchAlbumManifest`**：与相册页相同的清单拉取逻辑，读出已有 **`assets`**，再与本次新条目按 **`assetId`** 合并，然后 PUT 新的 **`albums/manifest.json`**（`MANIFEST_KEY` 与默认读取对象键对齐）。

---

## 6. 开发环境：Vite 代理与 URL 修正

- **`vite.config.ts`** 将 `/dev-oss-proxy/vrchat-png`、`/dev-oss-proxy/vrchat-img` 代理到对应 OSS 域名，减轻浏览器对 OSS 的 **CORS** 限制（GET/PUT）。  
- **`rewriteOssUrlForDevFetch`**（`src/lib/ossDevProxy.ts`）：仅在 **`import.meta.env.DEV`** 下把 OSS 域名改写为上述同源前缀。  
- **`resolveSignedUrlToAbsolute`**：把函数计算返回的相对路径、或错误指向 **`localhost`** 的链接，修正为 Bucket 根域名下的完整 HTTPS URL（可通过 **`VITE_OSS_SIGNED_URL_ORIGIN`** 指定 Bucket）。

生产构建不走开发代理；OSS 侧需配置与你的站点来源匹配的 **CORS**（GET 清单与图片、PUT 上传等）。

---

## 7. 环境变量一览（与相册相关）

| 变量 | 作用 |
|------|------|
| `VITE_ALBUM_MANIFEST_URL` | 可选；完整 HTTPS 直链清单，优先级最高（无效值忽略） |
| `VITE_ALBUM_MANIFEST_FILE` | 清单在 OSS 上的对象键，默认 `albums/manifest.json`，须与 FC `?file=` 及实际上传路径一致 |
| `VITE_OSS_SIGN_ENDPOINT` | 函数计算签名服务根 URL；不设则用代码内默认 |
| `VITE_OSS_SIGNED_URL_ORIGIN` | 相对签名 URL 拼到此前缀，默认 `vrchat-png` 外网域名 |
| `VITE_OSS_SIGN_ENDPOINT`（PUT） | 与 GET 共用同一常量；PUT 使用 `?put=1&key=` |

复制配置请参考仓库根目录 **`.env.example`**。修改 `VITE_*` 后需重新执行 **`npm run build`** 才会打进前端产物。

---

## 8. 函数计算约定（摘要）

| 用途 | 请求 | 响应 JSON 字段 |
|------|------|----------------|
| GET 临时读链（清单、图片） | `GET ...?file=<对象键>` | `signedUrl`，可选 `expire_in` |
| PUT 预签名（未用浏览器 RAM 时） | `GET ...?put=1&key=<对象键>` | `signedUrl` |

对象键中的路径分隔符应在签名服务侧与 OSS 实际 Key 一致；路径里不应出现错误的 `%2F` 编码问题（前端对明显错误会报错提示）。

---

## 9. 相关源码索引

| 模块 | 路径 |
|------|------|
| 清单拉取与归一化 | `src/lib/albumManifestFetch.ts` |
| GET 签名与缓存 | `src/lib/ossSignFetch.ts` |
| 签名 URL 规范化 | `src/lib/ossSignedUrl.ts` |
| 直链清单 URL（回退） | `src/lib/manifestUrl.ts` |
| PUT / 上传 | `src/lib/ossUpload.ts`、`src/lib/ossClientPresign.ts` |
| 开发代理改写 | `src/lib/ossDevProxy.ts`、`vite.config.ts` |
| 阅读页 | `src/pages/Album.tsx` |
| 管理端 | `src/pages/AlbumAdmin.tsx` |
| 加密算法 | `src/lib/albumCrypto.ts` |

---

## 10. 排错提示（简要）

- **`Unexpected token '<'`**：响应实为 HTML（常为 404 或 SPA `index.html`）。检查清单 URL、对象键、`VITE_ALBUM_MANIFEST_URL` 是否为合法绝对地址。  
- **清单 200 但 0 张 / 无图片请求**：检查 JSON 是否含 **`assets`**；条目是否含阅读端使用的 **`file`** 字段。  
- **Network 仍指向 `localhost` 的 OSS 路径**：检查 **`resolveSignedUrlToAbsolute`** 是否生效；函数计算是否返回相对路径或错误的 localhost 绝对 URL。  
- **上传 403**：多为 PUT **Content-Type** 与预签名不一致，或 OSS **CORS / 桶策略**。

---

*文档随实现演进；若修改清单字段或阅读端解密，请同步更新本节。*
