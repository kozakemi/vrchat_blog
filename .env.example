# ---------------------------------------------------------------------------
# 复制为 .env 或 .env.local 后按需填写。Vite 不会读取本文件名本身。
# 修改 VITE_* 后须重新 npm run build（清单与图片 URL 在构建时注入）。
# ---------------------------------------------------------------------------
#
# 可选：强制用直链拉清单（完整 https URL）。勿填 "/" 或相对路径。
# 不设此项时：清单与图片相同，经 VITE_OSS_SIGN_ENDPOINT 对 OSS 对象键换临时 URL（默认对象键见 VITE_ALBUM_MANIFEST_FILE）。
# VITE_ALBUM_MANIFEST_URL=
#
# 可选：清单在 OSS 上的对象键，须与 FC ?file= 及实际上传路径一致（默认 albums/manifest.json）
# VITE_ALBUM_MANIFEST_FILE=albums/manifest.json
#
# 可选：签名服务根 URL；不设则使用代码内默认函数计算地址
# VITE_OSS_SIGN_ENDPOINT=
#
# 可选：FC 返回相对路径签名时拼到此 Bucket 根（默认 vrchat-png）；勿改除非对象在别的 Bucket
# VITE_OSS_SIGNED_URL_ORIGIN=https://vrchat-png.oss-cn-beijing.aliyuncs.com
