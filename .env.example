# 复制为 .env 或 .env.local（推荐 .env.local，已在 .gitignore 中忽略 *.local）后按需填写。
#
# 相册清单仅在 OSS 时：填写清单对象的 HTTPS URL，开发与生产构建都会使用该地址（须配置 OSS CORS 允许当前来源 GET）。
# 若只写在 .env.production，则只有 npm run build 会注入；npm run dev 仍使用本地 public/albums/manifest.json。
# 若希望 dev 与线上一致，请把同一变量写在 .env 或 .env.local。
# VITE_ALBUM_MANIFEST_URL=https://your-bucket.oss-cn-beijing.aliyuncs.com/albums/manifest.json
#
# 可选：自定义 OSS 签名服务（相册读图临时 URL）
# VITE_OSS_SIGN_ENDPOINT=
VITE_ALBUM_MANIFEST_URL=https://vrchat-png.oss-cn-beijing.aliyuncs.com/albums/manifest.json