# 相册图片可解析数据与解析方式（VRChat PNG XMP）

本文档描述：在本项目的相册体系中，**单张图片可以自动解析/提取哪些信息**，以及在 **Node.js/TypeScript 的创建工具（CLI）** 与 **前端（浏览器）** 中各自应如何解析。

> 适用范围：以 VRChat 导出的 PNG 截图为主（通常内嵌 XMP 元数据）。  
> 约定：生成相册资源清单（manifest）时，若图片不存在 XMP：  
> - `takenAt` 默认使用**文件创建时间/修改时间**（在浏览器中可用 `File.lastModified` 作为近似）  
> - `worldId/worldName` 留空，待人工补全或后续编辑工具补充

---

## 1. 可以解析出的数据清单

### 1.1 基础文件信息（无需元数据）
| 字段 | 类型 | 来源 | 说明 |
|---|---|---|---|
| `fileName` | string | 文件名 | 例如 `VRChat_2026-03-01_22-12-35.355_2048x1440.png` |
| `ext` | string | 文件名 | `png` |
| `bytes` | number | 文件大小 | 用于展示/调试/阈值控制 |
| `width` | number | PNG IHDR | 图片宽度 |
| `height` | number | PNG IHDR | 图片高度 |
| `mime` | string | 推断 | `image/png` |

### 1.2 VRChat PNG 的 XMP 元数据（优先）
VRChat 常在 PNG 中写入 XMP（通常以 PNG `iTXt`/`tEXt` chunk 形式出现），常见字段如下（以你提供的示例为参照）：

| 字段 | 类型 | XMP 字段 | 示例 | 说明 |
|---|---|---|---|---|
| `creatorTool` | string | `xmp:CreatorTool` | `VRChat` | 生成工具 |
| `author` | string | `xmp:Author` | `Kozakemi` | 拍摄者/作者（可用于 UI） |
| `createDate` | string(ISO) | `xmp:CreateDate` | `2026-03-01T22:12:30.2107163+08:00` | 拍摄/生成时间（优先作为 `takenAt`） |
| `modifyDate` | string(ISO) | `xmp:ModifyDate` | 同上 | 修改时间（一般与 createDate 相同） |
| `dateTime` | string(ISO) | `tiff:DateTime` 或同类 | 同上 | 有些实现会额外写入 |
| `worldId` | string | `WorldID` | `wrld_...` | VRChat 世界 ID（用于分类视图） |
| `worldName` | string | `WorldDisplayName` | `FISHǃ ［RELEASE］` | 世界显示名称（可用于标题/分组名） |
| `worldAuthorId` | string | `AuthorID` | `usr_...` | 世界作者 ID（可选展示） |

> 注：XMP 的 namespace/字段可能随版本变化；解析时应“**尽量取到就用**”，取不到则回退。

### 1.3 从文件名推断（可选兜底）
VRChat 文件名通常包含时间与分辨率：
`VRChat_YYYY-MM-DD_HH-mm-ss.mmm_WxH.png`

可解析：
- `takenAt`（若 XMP 缺失，可从文件名推断）
- `width/height`（也可从文件名读，但更可靠的是读 IHDR）

### 1.4 画面内“叠加水印文字”（不推荐自动识别）
VRChat 截图底部可能烧录：
- 拍摄者、日期、世界名等

理论上可通过 OCR 识别，但：
- 成本高、准确率受语言/字体/背景影响
- 与 XMP 重复

**建议不做自动 OCR**，只作为人工核对手段。

---

## 2. 解析优先级与回退策略（推荐）

对每张图片生成清单时的推荐策略：

1. **解析 PNG 基础信息**：`width/height/bytes/mime`
2. **尝试提取 XMP**：
   - 若存在 `CreateDate` → `takenAt = CreateDate`
   - 若存在 `WorldID` → `worldId = WorldID`，`worldName = WorldDisplayName?`
3. 若 **XMP 不存在或缺关键字段**：
   - `takenAt`：  
     - CLI：优先用文件系统时间（建议用 `mtime` 或 `birthtime`，视平台而定）  
     - 浏览器：用 `File.lastModified`（毫秒时间戳）  
     - 也可尝试从文件名解析
   - `worldId/worldName`：留空（`null`/`""`），待人工填写

---

## 3. Node.js/TypeScript（CLI）解析方式

### 3.1 读取宽高（PNG IHDR）
方案 A（推荐）：使用成熟库读取 PNG 头部
- `fast-png` / `pngjs`（二选一）

示例（思路级伪代码）：
```ts
import { readFile } from "node:fs/promises";
import { decode } from "fast-png"; // 示例库名，实际以你选择为准

const buf = await readFile(path);
const png = decode(buf);
const width = png.width;
const height = png.height;
```

### 3.2 提取 XMP（PNG chunk：iTXt / tEXt）
PNG 文件由多个 chunk 组成：`length(4) + type(4) + data(length) + crc(4)`。

XMP 常见存放方式：
- `iTXt` chunk，keyword 可能是 `XML:com.adobe.xmp` 或 `xmp`
- 有时也会出现在 `tEXt`

建议做法：
1. 以二进制遍历 chunk
2. 若 type 为 `iTXt`/`tEXt`：
   - 解析 keyword（以 `\0` 分隔）
   - 若 keyword 命中 `XML:com.adobe.xmp` / `xmp`：
     - 取出 XML 字符串（注意 iTXt 可能压缩；VRChat 常见为未压缩）
3. 得到 XMP XML 后，用 XML 解析器提取字段

#### 3.2.1 Chunk 扫描（伪代码）
```ts
function readU32BE(b: Uint8Array, o: number) {
  return (b[o] << 24) | (b[o + 1] << 16) | (b[o + 2] << 8) | b[o + 3];
}

function extractXmpXmlFromPng(bytes: Uint8Array): string | null {
  // PNG signature 8 bytes
  let offset = 8;
  while (offset + 12 <= bytes.length) {
    const len = readU32BE(bytes, offset);
    const type = String.fromCharCode(...bytes.slice(offset + 4, offset + 8));
    const dataStart = offset + 8;
    const dataEnd = dataStart + len;
    // const crc = ... (可不校验)

    if ((type === "iTXt" || type === "tEXt") && dataEnd <= bytes.length) {
      const data = bytes.slice(dataStart, dataEnd);
      // 解析 keyword（以 0 结尾）
      const nul = data.indexOf(0);
      const keyword = nul >= 0 ? new TextDecoder().decode(data.slice(0, nul)) : "";
      if (keyword === "XML:com.adobe.xmp" || keyword === "xmp") {
        // iTXt 的结构比 tEXt 复杂，真实实现需解析 compression flag 等字段
        // 简化：在确认 VRChat 输出格式后再定制解析
        const xml = new TextDecoder().decode(data); // 真实实现需跳过头部字段
        return xml;
      }
    }
    offset = dataEnd + 4; // +crc
  }
  return null;
}
```

> 实战建议：先在 CLI 中把“VRChat PNG 的 iTXt 具体布局”打印出来确认；如果遇到压缩 iTXt，需要按规范解压（zlib）。

#### 3.2.2 XMP XML 解析
推荐使用轻量 XML 解析器，例如：
- `fast-xml-parser`

目标：提取（如存在）：
- `xmp:CreatorTool`
- `xmp:Author`
- `xmp:CreateDate`
- `xmp:ModifyDate`
- `WorldID`
- `WorldDisplayName`
- `AuthorID`

注意点：
- XML 带 namespace，解析时要能处理前缀
- 部分字段可能在不同 `rdf:Description` 节点中

### 3.3 文件时间（无 XMP 时的 takenAt）
在 CLI 侧（Node.js）可读到文件系统时间：
- `birthtime`（创建时间，部分系统不可靠）
- `mtime`（修改时间，通常更稳定）

建议策略：
1. 优先 XMP `CreateDate`
2. 否则 `takenAt = stat.mtime`（或配置为 `birthtime`）

---

## 4. 浏览器端解析方式（可选）

如果你希望用户在浏览器端直接导入本地图片并生成清单（不走 CLI），可以：

### 4.1 可获得的信息
- `File.name`
- `File.size`
- `File.lastModified`（近似“文件时间”）
- `ArrayBuffer`（可自解析 PNG chunk）

浏览器端无法直接获得真正的文件创建时间（跨平台差异），`lastModified` 通常够用。

### 4.2 解析流程
1. `const buf = await file.arrayBuffer()`
2. 扫描 PNG chunk → 找 `iTXt/tEXt` → 提取 XMP XML（同 CLI 思路）
3. 若无 XMP → `takenAt = new Date(file.lastModified)`

---

## 5. 输出到 Manifest 的字段建议

为支持“按时间排序”与“按世界分类”，建议每个 asset 在 manifest 中至少具备：
```json
{
  "assetId": "a_01HY...",
  "mime": "image/png",
  "bytes": 123456,
  "width": 2048,
  "height": 1440,
  "takenAt": "2026-03-01T22:12:30.2107163+08:00",
  "world": {
    "worldId": "wrld_ae001ea3-ed05-42f0-adf2-3d47efd10a77",
    "worldName": "FISHǃ ［RELEASE］"
  },
  "author": "Kozakemi",
  "source": {
    "xmp": true,
    "fileName": "VRChat_2026-03-01_22-12-35.355_2048x1440.png"
  }
}
```

当无 XMP 时：
- `takenAt`: 用文件时间（CLI: `mtime`；浏览器：`lastModified`）
- `world.worldId/worldName`: `null` 或 `""`
- `source.xmp`: `false`

---

## 6. 常见问题与坑

1. **XMP 字段不稳定**：务必做空值兼容，不要因为某字段缺失而失败。
2. **iTXt 压缩问题**：若遇到压缩 iTXt，需要按 PNG 规范解析并 zlib 解压。
3. **时间时区**：XMP 是 ISO8601，带 `+08:00` 之类时区；前端展示需统一到本地或固定时区。
4. **世界名字符**：可能包含全角符号/特殊字符（如 `ǃ`、`［］`），注意 JSON/URL 编码与 UI 换行。

